
AppKit/Cocoa
============

- To install AppKit/Cocoa framework, run:

        gnustep/core/gui$ make clean
        gnustep/core/gui$ make
        gnustep/core/gui$ sudo make install


Handling Errors
---------------

- If it gives you:

	././obj/make_services: error while loading shared libraries: libgnustep-base.so.1.23: cannot open shared object file: No such file or directory

* Then run:

	$ sudo ldconfig

